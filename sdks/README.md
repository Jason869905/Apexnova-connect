# Integration SDKs

此目录按语言提供 Integration contract 的开发体验封装。`schemas` 是跨语言事实来源，SDK 必须保持语义一致，不能引入只有单一语言能够表达的必需能力。

- `typescript`：面向 JavaScript/TypeScript 插件、Node 和应用运行时；
- `python`：面向 Python 插件及工作流平台运行时。

只在出现真实目标平台需求时增加新语言 SDK，避免维护没有使用者的镜像实现。

