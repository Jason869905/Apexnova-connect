{
  "name": "apexnova-connect",
  "version": "0.0.0",
  "private": true,
  "description": "Extensible integrations between Apexnova AI Hub and AI agents or automation platforms.",
  "license": "Apache-2.0",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.15.0"
  },
  "scripts": {
    "build": "pnpm --recursive --if-present run build",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "pnpm --filter @apexnova-connect/integration-sdk build && pnpm --filter @apexnova-connect/credential-store build && tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "7.0.2",
    "vitest": "4.1.11"
  }
}
